// Copy this file to Secrets.xcconfig and fill in your values from .env.local
// Secrets.xcconfig is gitignored — never commit it

// NOTE: xcconfig treats // as a comment — store host only, without https://
SUPABASE_HOST = your-project.supabase.co
SUPABASE_ANON_KEY = your-anon-key-here
